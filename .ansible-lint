---
# .ansible-lint

profile: production

exclude_paths:
  - .cache/ # implicit unless exclude_paths is defined in config
  - .github
  - inventory/pve0.custom.yml

# skip_list:
#   - fqcn-builtins
#   - package-latest
